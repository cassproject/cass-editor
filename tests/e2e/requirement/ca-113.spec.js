const { test } = require('../fixtures');

// CA-113: Permit configuration owner to define rollup processor
// Requirement not met: no rollup processor UI is exposed in the client
test.skip('CA-113: Rollup processor can be defined in configuration (not implemented in client)', () => { });
